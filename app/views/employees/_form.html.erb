<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" "form-group">
    
    <%= form.text_field :first_name, class:"form-control", placeholder:"First Name" %>
  </div>

  <div class="field" "form-group">
    
    <%= form.text_field :last_name, class:"form-control", placeholder:"Last Name"%>
  </div>

  <div class="field" "form-group">
    
    <%= form.text_field :position, class:"form-control", placeholder:"Position"%>
  </div>

  <div class="field" "form-group">
    
    <%= form.text_field :department, class:"form-control", placeholder:"Department"%>
  </div>

  <div class="field" "form-group">
    
    <%= form.text_field :university, class:"form-control", placeholder:"University"%>
  </div>

  <br>
  <div class="actions">
    <%= form.submit class:"btn btn-dark" %>
  </div>
<% end %>
